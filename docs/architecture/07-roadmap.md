# 07 — Roadmap e incrementos (Fase J)

> **Revisado con Juan (2026-09-09).** Esta versión reemplaza el roadmap inicial: el Incremento 1 queda aún más angosto de lo propuesto originalmente — ni siquiera incluye Project Knowledge/Session Memory/Decision Records todavía — para poder validar la hipótesis central lo antes posible y con datos medibles, no solo con intuición.

Principio: cada incremento debe terminar en algo que se pueda ejecutar y probar de punta a punta. La hipótesis central que hay que probar primero, y de la que depende todo lo demás, es angosta y concreta:

```
Proyecto
 ↓
Contexto persistente (mínimo)
 ↓
Context Pack
 ↓
IA web (DeepSeek / Claude / ChatGPT)
 ↓
Respuesta
 ↓
Import (parser de 3 capas)
 ↓
Diff
 ↓
Apply
```

Si esta cadena funciona, el resto del roadmap es incrementar sofisticación sobre una base ya validada. Si no funciona, ninguna otra pieza (Git, Claude Code, memoria persistente) importa todavía.

## Paso 0 — Validación manual del contrato de respuesta (sin código) — ✅ HECHO (2026-09-09)

Se tomó una tarea real de "Camino al Deporte" (calcular `TamanoPaquete` a partir del carrito, en vez de usar `MEDIANO` fijo), se redactó a mano un Context Pack real (~3,850 tokens, 5 archivos/fragmentos con score de relevancia) siguiendo el contrato de 06, y se probó contra DeepSeek Web y Claude Web (ChatGPT).

**Resultado: hipótesis central validada — con matices reales que ya están incorporados al diseño.**

```
✅ Context Pack comprensible sin explicación adicional
✅ Ambos modelos ubicaron los archivos correctos con contexto reducido
✅ El contrato DEVPILOT_CHANGE / SEARCH-REPLACE es entendible
⚠️ Los modelos pueden inventar decisiones de negocio no documentadas
   (DeepSeek: umbrales de volumen/peso que no estaban en el pack)
⚠️ Un fallback silencioso mal pensado puede colar un riesgo de negocio
   (DeepSeek: dimensiones desconocidas → 0 → clasificado como CHICO)
⚠️ El formato de patch puede salir ligeramente inválido incluso con buen
   razonamiento (ChatGPT: marcadores SEARCH/REPLACE mal formados)
```

Ninguna de las dos respuestas debía aplicarse tal cual llegó — y ese hallazgo, con evidencia real de dos modelos distintos fallando por razones distintas, es lo que justificó separar un `ChangeValidator` explícito del `ChangeParser` y añadir la sección "Decisiones del negocio: confirmadas vs. abiertas" al propio Context Pack. Ambos cambios ya están incorporados en 02, 03, 04 y 06 — no son trabajo pendiente de diseño, son parte del Incremento 1 desde ahora.

**Segunda ronda (misma tarea, mismo Context Pack sin el contrato endurecido aún):** se corrió la lógica del `ChangeValidator` a mano contra los archivos reales del proyecto. Confirmó que el divisor `=======` de los patches puede perderse en el trayecto (probable causa: Markdown lo interpreta como encabezado Setext) sin que el contenido de `SEARCH` en sí esté mal — y expuso el hallazgo más serio hasta ahora: una respuesta sin marcadores de patch que presenta solo un fragmento de `schema.prisma` con el comentario de la ruta real, que de haberse tratado como "reemplazo de archivo completo" habría borrado el esquema entero. Se agregaron dos checks nuevos al `ChangeValidator` (`whole-file-boundary-match`, `naming-convention`) y una recuperación heurística por prefijo más largo (siempre a `warning`, nunca a `valid`) para el divisor perdido. Detalle completo en 06.

## Incremento 0 — Andamiaje

- Monorepo pnpm (`apps/cli`, `packages/core`, `packages/storage`, `packages/shared`), TypeScript estricto, lint, build.
- `devpilot --version` funcionando end-to-end como humo del setup.

## Incremento 1 — "Proof of Context"

Objetivo único: demostrar la cadena completa Proyecto → Contexto → IA web → Respuesta → Import → Diff → Apply, con la menor cantidad de piezas posible.

- ✅ **HECHO (2026-09-09) — Scanner + `devpilot project add`/`project list`.** `devpilot project add <ruta>` → Scanner detecta stack (lenguaje/framework/package manager/ORM/base de datos/Docker/Git presente) → Project Snapshot persistido (registro global en `~/.devpilot/registry.db` + estado y snapshot local en `.devpilot/devpilot.db` + `.devpilot/state/snapshot.json`, ver 03). Implementado en `@devpilot/storage` (paths/schema/connection/repos de `projects` y `project_state`, FileStore de snapshot) y `@devpilot/core` (`fileWalker`, `scanner`, `projectService`), expuesto en el CLI como `project add`/`project list`. Verificado de punta a punta: build + lint limpios, y `project add` corrido con éxito tanto sobre el propio repo de DevPilot como sobre el proyecto real Camino al Deporte (296 archivos, detectó correctamente Next.js + npm + Prisma + PostgreSQL + el commit de Git actual). De paso resuelto: la dependencia circular potencial entre `@devpilot/core` y `@devpilot/storage` (los tipos de dominio se movieron a `@devpilot/shared`, que no depende de nada — ver `packages/shared/src/domain/types.ts`), y el desalineamiento entre el esquema SQL de `projects` (03) y el tipo `Project` (le faltaban `vcs` y `updated_at`, ya corregido en 03 y en el código).
- ✅ **HECHO (2026-09-09) — Context Planner + `devpilot context`.** `devpilot context "<tarea>"` → Context Planner **simple y medible, no "superinteligente"**: coincidencias de texto/nombre + imports directos (Nivel 1 + Nivel 2 de 04, nada de AST ni Git todavía) → Relevance Scoring v1 con razones explicables → Token Estimate → Context Pack exportado a Markdown y JSON, **siempre persistido en `.devpilot/context/`** (nunca solo mostrado en pantalla — se audita después qué sabía la IA), y copiado al portapapeles (best-effort: `clip`/`pbcopy`/`xclip`/`xsel` nativos, sin dependencia nueva). Implementado en `@devpilot/core/context` (`keywords`, `importGraph`, `relevancePlanner`, `tokenEstimate`, `responseContract`, `contextPackBuilder`, `markdownRenderer`, `contextService`) y expuesto en el CLI (`--confirmed`/`--open`/`--constraint` repetibles para las decisiones de negocio que en este incremento el usuario escribe a mano, `--max-files` para la compactación). El `responseInstructions` embebido en cada pack ya es la Capa 1 endurecida completa (06), incluida la sección obligatoria "Decisiones que asumí".
  - **Validado contra el propio caso del Paso 0**: corrí `devpilot context` con el texto exacto de la tarea original sobre Camino al Deporte real, y el resultado automático coincide en lo esencial con el pack armado a mano en el Paso 0 — `backend/src/routes/tienda/envios.js` y `backend/src/routes/tienda/pedidos.js` quedaron en el top del ranking, `backend/prisma/schema.prisma` y `docs/ARQUITECTURA.md` también aparecieron (los mismos archivos que se habían elegido a mano). Esto es la primera confirmación de que el Context Planner heurístico (Nivel 1+2) reproduce, sin intervención humana, una selección de archivos que ya sabíamos que funcionaba.
  - **Dos bugs reales encontrados y corregidos durante esa validación** (no en el diseño, en la implementación — ambos con evidencia real, mismo espíritu que el Paso 0): (1) los imports con especificador `.js` que en realidad resuelven a un archivo `.ts` (el propio estilo de este monorepo, TypeScript+NodeNext) no se resolvían — el grafo de imports del Nivel 2 se quedaba corto; (2) el Nivel 2 sumaba una contribución de `import-distance` por cada archivo semilla que importara un mismo archivo "hub" (ej. `db.js`, importado por decenas de rutas), lo que inflaba archivos genéricos por encima del archivo realmente relevante — corregido tomando el mejor puntaje por categoría de señal en vez de sumar todas las instancias. Un tercer ajuste (no bug, calibración): las coincidencias de texto ahora pesan distinto según si el término es un identificador de código (`TAMANO_CHECKOUT_DEFAULT`) o una palabra genérica del dominio (“tienda”, “checkout”) — sin esto, palabras genéricas repetidas en un módulo entero saturaban el score igual que el término específico que la tarea pedía modificar.
  - `context_packs` (03) sí se persiste como fila de auditoría desde ya; `sessions` queda con `session_id = NULL` a propósito — Session Memory real es Incremento 2, y el `sessionId` que sí lleva el objeto `ContextPack` en memoria es solo un identificador de esta invocación.
- El usuario pega el pack en DeepSeek Web / Claude Web / ChatGPT, conversa, y copia la respuesta.
- ✅ **HECHO (2026-09-09) — `ChangeParser` + `ChangeValidator` + `devpilot import`.** `devpilot import <archivo-o-texto>` (archivo o texto pegado directo) → `ChangeParser` (Capa 2 de 06) reconoce, en orden, bloques `<DEVPILOT_CHANGE>`, Markdown con encabezado `### File:` + fence, y como último recurso un fence genérico con una ruta plausible en su primera línea (`format: 'fence-only'`) — con toda la tolerancia sintáctica documentada en 06 (marcadores con prefijo de comentario o espacios internos, recuperación heurística por prefijo cuando falta el divisor `=======`, dedent tolerante). Texto que no calza con ningún patrón nunca se descarta, se acumula en `unparsedNotes`. `ChangeValidator` (Capa 3) corre 7 checks (`path-exists`, `operation-valid`, `search-match`, `marker-well-formed`, `whole-file-boundary-match`, `naming-convention`, `undocumented-decision`) y clasifica cada cambio en `valid`/`warning`/`reject` con `confidence` 0-100, exactamente con la combinación de reglas de 06 (los checks estructurales rechazan, los demás solo generan `warning`, `fence-only` nunca llega a `valid` sin ojos humanos). Cada propuesta evaluada se persiste en `file_change_proposals` (auditoría — `applied` sigue en 0, `devpilot apply` todavía no existe). Implementado en `@devpilot/core/context` (`matching`, `markerRecovery`, `changeParser`, `undocumentedDecision`, `changeValidator`, `importService`) y `@devpilot/storage` (`fileChangeProposalRepo`, más `getLatestContextPack`/`getContextPackById`), expuesto en el CLI como `devpilot import`.
  - **Validado con 6 respuestas sintéticas** diseñadas para reproducir uno a uno los hallazgos reales del Paso 0 (06, "Segunda ronda de evidencia"): patch limpio → `valid`; marcador con prefijo de comentario y divisor perdido → `warning` vía recuperación por prefijo; fence sin marcadores con contenido que no calza con el archivo real → `reject` vía `whole-file-boundary-match` (el caso que, sin este check, habría borrado un archivo completo — Hallazgo C); ese mismo fence pero con el contenido real exacto del archivo → nunca `valid`, tope de confianza por ser `fence-only`; umbral numérico no documentado → `warning` vía `undocumented-decision`; `SEARCH` que ya no existe en el archivo real → `reject` vía `search-match`. Las 6 coincidieron con lo esperado.
  - **Validado además con una respuesta realista contra Camino al Deporte real** (reusando el Context Pack ya generado en la fase anterior sobre `backend/src/routes/tienda/envios.js`): una respuesta con 3 bloques `<DEVPILOT_CHANGE>` (dos con marcadores limpios, uno con el patrón real de ChatGPT del Paso 0 — `# <<<<<<< SEARCH` + `> > > > > > > REPLACE` sin divisor) y una sección `## Decisiones que asumí` con un umbral de negocio inventado (5 artículos / 8kg → GRANDE). Resultado: 2 `valid` + 1 `warning` (recuperación por prefijo), exactamente como se esperaría a mano.
  - **Tres bugs reales encontrados y corregidos durante esa validación** (ninguno visible en las 6 pruebas sintéticas — solo aparecieron contra código y texto reales, mismo patrón que en las dos fases anteriores): (1) en `fence-only`, la primera línea usada para identificar la ruta (`// backend/src/routes/tienda/envios.js`) se dejaba dentro del `newContent` propuesto — al compararlo contra el archivo real, `whole-file-boundary-match` fallaba por un desfase de una línea aunque el resto del contenido fuera un reemplazo completo y exacto, degradando un `warning` esperado a un `reject` (falso negativo de seguridad, no falso positivo: más grave, porque descarta una propuesta válida en vez de sobre-advertir); (2) el regex de la sección `## Decisiones que asumí` exigía el paréntesis final (`(no confirmadas por el contexto)`) del formato recomendado en 06 — el `?` de `\([^)]*\)?` solo hacía opcional el paréntesis de *cierre*, no el de apertura — así que una respuesta real sin ese paréntesis (variación de redacción perfectamente razonable) caía completa en `unparsedNotes` en vez de extraerse a `assumedDecisions`; sigue mostrándose (nunca se pierde), pero no con la prominencia que 06 exige para este campo específicamente. (3) `undocumented-decision` comparaba el número propuesto contra el pack completo con `String.includes` — un simple substring, así que "5" se consideraba "documentado" si aparecía dentro de "2025" o de cualquier otro número más largo en cualquier archivo del pack; corregido para exigir que el número aparezca como token completo (no como parte de otro número). **Limitación conocida, no resuelta de raíz:** incluso con esa corrección, probado contra el pack real de Camino al Deporte, los umbrales sintéticos de la prueba (5, 8) sí aparecían como números *sueltos* en otro lugar del pack sin relación alguna (una clase de Tailwind `p-5`, una talla de tenis "8" en datos de producto) — el check es barato y heurístico a propósito (06 lo dice explícitamente), pero para números de un solo dígito en un proyecto real con bastante texto/código, el riesgo de falso negativo es mayor de lo que el diseño original asumía. Documentado aquí como candidato a refinar en Incremento 2 (por ejemplo, exigir alguna palabra de dominio de negocio cerca del número, no solo que el número mismo no aparezca en ningún lado).
- ✅ **HECHO (2026-09-09) — `devpilot diff`.** `devpilot diff` reconstruye y muestra el diff real de cada propuesta ya importada (`file_change_proposals`) contra el estado actual del archivo — sigue sin tocar disco, es la pieza puramente de lectura entre `devpilot import` y `devpilot apply`. Los `valid` se muestran etiquetados como listos para aplicar, los `warning` como una decisión pendiente (con el detalle de qué check no pasó), y los `reject` se explican (incluyendo el diff, cuando se puede reconstruir, para que se vea concretamente qué se habría roto) pero nunca se ofrecen. Para `patch`, la reconstrucción reutiliza la misma tolerancia de indentación que ya usaba `search-match` (`findDedentTolerantMatch`, nueva función en `matching.ts`, misma noción de "coincide" que `dedentTolerantIncludes` — comparten el mismo patrón de regex para no arriesgar que diverjan) para ubicar la región real y reemplazarla por el bloque `REPLACE`; para `format: 'diff'` (la IA ya entregó un diff unificado) se muestra tal cual, reetiquetado — nota aparte más abajo. El diff en sí se genera con `createTwoFilesPatch` de `diff` (jsdiff), única dependencia nueva de este incremento. Implementado en `@devpilot/core/context` (`diffService.ts`) y expuesto en el CLI como `devpilot diff` (coloreado ANSI mínimo, sin dependencia nueva para eso).
  - **Extensión necesaria a `file_change_proposals`/`devpilot import`, encontrada al implementar esto, no un bug de `import` en sí**: la tabla solo guardaba metadata (ruta, operación, formato, estado, confianza...) — nunca el contenido real de la propuesta (`newContent`/`diff`/`patch`), que tampoco se conserva en ningún otro lado (la respuesta cruda de la IA no se persiste). Sin eso, `devpilot diff` no tiene con qué reconstruir nada. Se agregó `.devpilot/changes/<id>.json` (`changeProposalStore.ts`, mismo principio que `contextPackStore.ts`: contenido largo en archivo, la fila en SQLite solo apunta a la ruta) y la columna `proposal_path` a `file_change_proposals` (03, `schema.ts`) — con migración (`ensureFileChangeProposalPathColumn` en `connection.ts`, vía `PRAGMA table_info` + `ALTER TABLE`) para bases de proyecto ya existentes de antes de este cambio. Filas viejas sin `proposal_path` (o con una ruta que ya no se puede leer) se señalan en el diff pidiendo re-correr `devpilot import`, en vez de romper el comando completo.
  - **Bug real de portabilidad encontrado y corregido al validar esto contra Camino al Deporte real, en una sesión distinta a la que generó el Context Pack**: `context_packs.markdown_path`/`json_path` (y ahora `file_change_proposals.proposal_path`) se guardaban como rutas **absolutas**, calculadas en el momento de escribir el archivo. Eso rompe justo la garantía de portabilidad que 03 promete explícitamente ("si el usuario mueve o comparte la carpeta... su conocimiento viaja con ella"): si el proyecto se mueve, o si la misma carpeta se abre con un prefijo de montaje distinto (el caso real que expuso esto — el puente de dispositivo de Cowork monta cada carpeta conectada bajo un directorio específico de esa sesión, `/sessions/<id>/mnt/<carpeta>`, que cambia entre sesiones aunque la carpeta real en Windows sea la misma), la ruta absoluta guardada deja de servir aunque el archivo siga estando ahí, relativo a la nueva raíz del proyecto. Corregido guardando ambas rutas **relativas a `rootPath`** (`contextService.ts`, `importService.ts`) y resolviéndolas contra el `rootPath` *actual* al leerlas (`importService.ts`, `diffService.ts`) — nunca el de cuando se generaron. Verificado moviendo un proyecto de prueba a una ruta absoluta completamente distinta a mitad de la prueba y confirmando que `devpilot diff` seguía reconstruyendo todo correctamente. Filas ya insertadas antes de este fix quedan con su ruta absoluta vieja (irrecuperable — apuntan a un directorio que ya no existe) pero ahora fallan con un mensaje explicativo en vez de un stack de error crudo.
  - **Validado con dos baterías sintéticas** (creadas para este incremento, sobre un proyecto fixture aislado): la primera cubre `create`/`patch`/`delete`/`edit` con `format: 'diff'` (diff crudo de la IA mostrado tal cual) todos en `valid`/`warning`, confirmando que cada operación reconstruye el contenido correcto; la segunda cubre los dos casos de `reject` — `search-match` fallido (ni siquiera se puede reconstruir el diff, y el mensaje de error lo dice explícitamente en vez de fallar en silencio) y `whole-file-boundary-match` fallido (aquí SÍ se pudo reconstruir el diff, mostrando visualmente cómo el cambio propuesto habría borrado casi todo el archivo real — la evidencia visual de por qué se rechazó, no solo el nombre del check).
  - **Validado además contra Camino al Deporte real**, regenerando el Context Pack de la tarea del Paso 0 y reimportando una respuesta con los mismos tres patches de la validación de `devpilot import` (dos con marcadores limpios, uno reproduciendo el patrón real de ChatGPT sin divisor — esta vez con el patrón corregido: contenido viejo y nuevo concatenados sin separador dentro del bloque, con el marcador `REPLACE` solo al final, que es como realmente ocurre el Hallazgo A, no con un marcador `REPLACE` a mitad como en un primer intento fallido de reproducirlo a mano que sí se detectó y se descartó de la base antes de dar esto por válido) → 1 `valid` + 2 `warning`, y `devpilot diff` reconstruyó los tres diffs correctamente, incluida la recuperación por prefijo del tercero (confirma que la reconstrucción para `patch` con `markerOrigin: 'recovered-prefix'` funciona igual que con marcadores limpios).
  - **Superficie sin cubrir, no bloqueante, documentada para más adelante**: `format: 'diff'` (la IA responde con un diff unificado directo) no tiene hoy ningún check del `ChangeValidator` (06) que lo compare contra el archivo real — ni `search-match` ni `whole-file-boundary-match` aplican a esa combinación de `operation`/`format`. `devpilot diff` lo muestra tal cual porque no hay nada que reconstruir, pero eso significa que un diff de la IA que ya no aplica limpio contra el archivo actual no se detecta hasta `devpilot apply` (cuando exista) intente aplicarlo de verdad. Candidato a un check nuevo en el Incremento 2, o a resolverlo directamente en `devpilot apply` validando que el diff aplique en seco antes de tocar disco.
- ✅ **HECHO (2026-09-09) — Tool Engine mínimo (`PermissionGuard`) + `devpilot apply`.** `devpilot apply` es la primera vez que DevPilot escribe algo en el disco del usuario, así que es también la primera implementación real (no solo diseñada en 05) del Tool Engine: `PermissionGuard` (`packages/core/src/tools/permissionGuard.ts`) clasifica cada operación en riesgo `write` (`create`/`edit`/`patch`) o `delete`, y siempre exige confirmación explícita — no existe un "aprobar todo" implícito, tal como 05 lo prohíbe para v1. `PathGuard` está integrado dentro del mismo `evaluatePermission` (no como módulo separado: en la práctica es un solo chequeo de "¿la ruta resuelta cae dentro de la raíz del proyecto?") y se re-evalúa dos veces — al armar el plan y otra vez justo antes de escribir, en `applyChange`, defensa en profundidad real y no solo cosmética. Cada intento, se apruebe o no, se audita en `tool_invocations` (la tabla ya existía desde el Incremento 0 sin usarse; este es su primer consumidor real). `devpilot apply` solo ofrece `valid` por defecto; `warning` requiere `--include-warnings` o `--only <id>` explícito (el usuario ya los revisó con `devpilot diff`); `reject` nunca se ofrece, ni siquiera con `--only` — ahí no hay bypass posible. `--yes-to write` pre-aprueba visualmente las escrituras sin preguntar una por una, pero `delete` nunca es bypassable por ningún flag (requisito explícito de 05); re-correr `apply` sobre propuestas ya aplicadas las salta (`applied` en `file_change_proposals`), así que es seguro repetir el comando. La reconstrucción de contenido para las 4 operaciones (`create`/`edit`/`delete`/`patch`, más `format: 'diff'`) se unificó en un módulo nuevo y compartido, `changeReconstruction.ts` (antes vivía duplicada/especializada dentro de `diffService.ts`), y `devpilot apply` reutiliza exactamente el mismo diff que ya mostraba `devpilot diff` antes de pedir la confirmación. Implementado en `@devpilot/core/context` (`changeReconstruction.ts`, `applyService.ts`), `@devpilot/core/tools` (`permissionGuard.ts`, paquete nuevo dentro de core), `@devpilot/storage` (`toolInvocationRepo.ts`, `markFileChangeProposalApplied`), expuesto en el CLI como `devpilot apply` (mismo coloreado ANSI del diff que `devpilot diff`, confirmación por `readline`).
  - **Cierra la superficie sin cubrir que había quedado documentada al construir `devpilot diff` (línea de arriba, "Superficie sin cubrir")**: `format: 'diff'` ahora sí se valida antes de escribir — `changeReconstruction.ts` usa `applyPatch(oldContent, proposal.diff, { fuzzFactor: 0 })` de jsdiff no solo para aplicar el diff sino como chequeo de vigencia: si `applyPatch` devuelve `false` (el diff ya no calza limpio contra el contenido actual del archivo), `devpilot apply` lo reporta como error explícito en vez de escribir algo corrupto o fallar a mitad de escritura. Confirmado con una prueba directa de jsdiff antes de integrarlo: `applyPatch` devuelve `false` de forma confiable contra contenido que cambió, y funciona igual con o sin los encabezados `---`/`+++` del diff.
  - **Bug real encontrado y corregido durante las pruebas de este mismo incremento, no relacionado con el Tool Engine ni con la reconstrucción — un bug de Node, no de lógica de negocio**: la primera versión de la confirmación interactiva del CLI creaba una interfaz de `readline` nueva por cada pregunta (`createInterface` + `rl.question` + `rl.close()` dentro de una función `confirm()` llamada una vez por cambio). Probado con respuestas simuladas por stdin (`printf 'y\ny\ny\n' | devpilot apply`), el comando aplicaba el primer cambio y luego se quedaba colgado esperando la segunda respuesta para siempre, aunque el pipe ya la traía. Causa: `rl.close()` descarta el buffer interno de líneas de esa interfaz — cualquier línea ya disponible en el pipe pero no consumida todavía por esa instancia se pierde al cerrarla. Reutilizar una sola interfaz para todo el comando (en vez de una por pregunta) pareció resolverlo, pero expuso un segundo problema, más sutil: con stdin *no interactivo* (pipe, redirección, o `yes |`), Node lee todo el input disponible de una sola vez y la interfaz de `readline` se cierra sola en cuanto el pipe llega a EOF — como nuestro código hace trabajo asíncrono real entre pregunta y pregunta (reconstruir contenido, escribir a disco), la interfaz ya está cerrada para cuando se necesita la segunda pregunta, y `rl.question()` lanza `readline was closed` aunque el pipe traía de sobra las respuestas. Confirmado con un repro mínimo aislado (sin nada de DevPilot de por medio) antes de tocar el código real, para descartar que fuera un bug de nuestra lógica. **Corregido con un `Confirmer` que distingue los dos casos**: con una terminal real (`process.stdin.isTTY`), sigue preguntando una por una con `readline`, como espera un humano tecleando; sin TTY, lee TODO el stdin de una sola vez por adelantado (ya está completo en el pipe) y consume una línea por confirmación desde una cola en memoria, sin más llamadas a `readline` — si el input se agota antes de lo esperado, se trata como "no" con un aviso explícito en vez de colgarse. Esto no es solo un problema de cómo probamos `apply`: cualquier uso real de DevPilot en un script, en CI, o con `yes | devpilot apply --yes-to write` habría tenido el mismo problema silencioso.
  - **Validado con una batería sintética de 4 cambios** sobre un proyecto fixture aislado (`create`, `delete`, `patch` con marcadores limpios diseñado para además disparar `undocumented-decision`, y `edit` con `format: 'diff'`): comportamiento por defecto (solo se ofrecen los `valid`, el `warning` se salta con el motivo explicado), `--include-warnings` (se ofrece el `warning`, primero rechazado con "n" para confirmar que el rechazo del usuario también se audita y no toca disco, luego aceptado con "y"), re-correr `apply` sin flags (los 4 ya aplicados se saltan como `already-applied`, cero preguntas), `--only <id>` sobre el `reject` de una segunda batería con un intento deliberado de escape de ruta (`../outside.js`, ya bloqueado desde `devpilot import` por `path-exists`) confirmando que ni con `--only` se puede forzar, y `--yes-to write` (la escritura se pre-aprueba sin tocar stdin en absoluto — se verificó que el `Confirmer` no-TTY ni siquiera intenta leer el pipe para esa entrada — mientras el borrado en la misma corrida sigue preguntando, tal como exige 05). Los 4 archivos en disco y las filas de `file_change_proposals.applied`/`tool_invocations` quedaron exactamente como se esperaba en cada paso.
  - **Validado además contra Camino al Deporte real**: por seguridad, en vez de reaplicar las propuestas de prueba ya guardadas de la validación de `devpilot diff` (esas eran fixtures sintéticas para probar reconstrucción de patches, no cambios reales que alguien pidió), se generó un Context Pack y una propuesta `create` nuevos y deliberadamente inocuos (un archivo Markdown de prueba, fuera de cualquier código real del proyecto), se aplicó de verdad con `devpilot apply` contra el proyecto real en Windows a través del puente de dispositivo — confirmando que la escritura real a disco funciona igual con una ruta de Windows montada vía FUSE que con el filesystem de Linux del sandbox — y después se generó y aplicó un segundo cambio (`delete`) para borrar ese mismo archivo, confirmando la operación de borrado real también contra el proyecto real. `git status` después de ambas corridas no muestra ningún residuo en el árbol de trabajo (solo `.devpilot/`, que es esperado).
- **Benchmark automático por tarea** (`task_benchmarks`, ver 03/04): tokens enviados, archivos incluidos vs. usados, cambios importados vs. aplicados, resultado. Esto es parte del Incremento 1, no un "nice to have" posterior — es como se mide si la hipótesis central realmente funciona.

Lo que **no** entra en el Incremento 1: Project Knowledge, Session Memory, Decision Records (pasan al Incremento 2), Git más allá de detectar que existe, símbolos/AST, Claude Code, análisis semántico.

**Criterio de éxito:** repetir en código lo que ya se demostró a mano en el Paso 0 — Context Pack real, respuesta de un chat web real, y que el `ChangeParser` + `ChangeValidator` clasifiquen correctamente los cambios de DeepSeek/ChatGPT como `warning`/`reject` en vez de aplicarlos ciegamente — con métricas de benchmark que respalden la afirmación de ahorro de contexto.

## Incremento 2 — "Project Memory"

- Project Knowledge (`knowledge_docs` + `.devpilot/knowledge/*.md`), inicialmente poblado por heurísticas del Scanner (`source: 'inferred'`) y editable a mano (`source: 'manual'`).
- Session Memory real (más allá de un log plano) y promoción de eventos de sesión relevantes.
- Decision Records (`devpilot decide "<título>"`).
- Refresco incremental de Project State.

## Incremento 3 — "Git Intelligence"

- `git status` / `git diff` / `git log` vía `GitAdapter` (02).
- Reindexado incremental basado en `lastIndexedCommit` + `git diff --name-status` (evita rescanear todo el árbol).
- Extracción de símbolos (AST ligero) → Nivel 3 del Context Planner.
- Nivel 4: boost de relevancia por recencia en Git y afinidad con mensajes de commit.
- Context Compaction para packs 🔴.

## Incremento 4 — "Claude Code"

- `devpilot analyze --deep` → `ClaudeCodeProvider` en modo de solo lectura/plan → propone contenido para `knowledge_docs` (arquitectura, módulos, reglas de negocio, decisiones detectadas) → el usuario aprueba el diff antes de escribir (mismo flujo de aprobación del Tool Engine).
- `lastDeepAnalysisCommit` para saber qué cambió desde el último análisis profundo.

## Incremento 5 — "Browser Bridge"

- `apps/browser-extension` con adapters específicos por sitio (`deepseek-web`, `claude-web`, `chatgpt-web`) que automatizan el copiar/pegar físico como conveniencia de UX. El parseo de respuesta sigue siendo el mismo contrato de tres capas de 06 — la extensión nunca es una dependencia funcional, solo ahorra clics.

## Más adelante (exploratorio)

- `devpilot analyze` con otros proveedores (DeepSeek API, OpenAI API) detrás de la misma interfaz `AIProvider`, siempre BYOAI.
- Desktop UI (Electron + React + Vite) como cliente fino de la API programática de `core`.
- Soporte multi-lenguaje más allá de TypeScript/JavaScript.
- Capa semántica local opcional (Nivel 5), solo si el benchmark del Incremento 1-3 demuestra que el scoring heurístico es insuficiente en la práctica.

---

**Siguiente paso concreto:** el Paso 0 ya se hizo y el resultado fue alentador (con matices ya incorporados al diseño). El primer código que escribimos es el Incremento 0 + el Scanner del Incremento 1.
