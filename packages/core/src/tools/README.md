FileTools, SearchTools, TerminalTool, GitTools, PermissionGuard, PathGuard — ver `docs/architecture/05-tool-engine-and-security.md`.

Se implementa en el **Incremento 1** (lo mínimo para `apply`: FileTools + PermissionGuard + PathGuard); TerminalTool y GitTools completos llegan en incrementos posteriores según lo necesiten (ver `docs/architecture/07-roadmap.md`).
